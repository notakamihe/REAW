<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <meta
      http-equiv="Content-Security-Policy"
      content="script-src 'self' 'unsafe-inline'"
    />
    <title>React Audio Workstation</title>
  </head>
  <body>
    <div id="root"></div>
    <script>
      document.body.addEventListener("dragenter", preventDefaultEffect);
      document.body.addEventListener("dragover", preventDefaultEffect);

      function preventDefaultEffect(e) {
        e.stopPropagation();
        e.preventDefault();
        e.dataTransfer.effectAllowed = "none";
        e.dataTransfer.dropEffect = "none";
      }
    </script>
  </body>
</html>